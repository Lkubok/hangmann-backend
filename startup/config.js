const config = require('config');
const {SERV_JWT} = process.env;                


module.exports = function(){
  /*   if(!config.get(SERV_JWT)){
        throw new Error(`FATAL ERROR: serv_jwt is not defined.`);
    } */
}
